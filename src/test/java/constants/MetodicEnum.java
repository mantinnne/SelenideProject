package constants;

public enum MetodicEnum {
        Destructive(35),
        Oppositional(34);

        public final int code;

        MetodicEnum(int code) {
            this.code = code;
        }

}
